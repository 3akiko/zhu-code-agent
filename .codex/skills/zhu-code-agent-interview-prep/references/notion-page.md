# Notion「📔 简历分析」页结构速查

- 页面：📔 简历分析，id `1b1f7b2f17d6801eaae0e8e4b100a56f`，父页面「工作之路」。
- 结构（自顶向下）：
  - `# 自我介绍`（markdown 代码块）、`# 项目经验`、`## 一、麦穗召回链路优化`…`### plan模式`（图片签名 URL 导致 fetch 中间截断）
  - 面试问答区（页面末尾，2026-08-13 起追加）：
    - `### 面试 Q1：你在项目中遇到的最大的难点是什么？你是怎么一步一步解决的？`
    - `### 追问补充：Q1 的追问小问题 & 一句话总结`（```plain text 代码块，3 条 Q★）
    - `### 面试 Q2 / Q4 / Q5`（Q2 含 LangChain/LangGraph 对比；Q4 不含"协议感知占用"；Q5 含非交互模式 -p）
- 追加规范：
  - 三级标题 `### 面试 Q{N}：…` + 正文；追问/一句话总结用 fenced code block。
  - 锚点选择：取现有某段末尾一行做 old_str，new_str = old_str + 空行 + 新内容；避免包含引号/转义复杂的整段。
  - blockquote（`>` 开头）后接标题必须 `\n\n` 隔开，否则可能被引用块吞并。
- 定位技巧：`notion-fetch` 输出大且中间截断时，用 `notion-search`（query 命中短语 + page_url=页面）拿到小节附近的精确文本作锚点。
