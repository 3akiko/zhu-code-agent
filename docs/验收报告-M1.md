# zhuCodeAgent M1 验收报告

- 验收日期：2026-08-07
- 依据：`docs/milestones/m1/checklist.md`（已批准）
- 结果：**通过（57/57 自动化测试全绿 + 冒烟验证）**，1 项可选人工项待真实 API 密钥

## 实现完整性

- [x] C1 配置加载（AC5）—— 证据：`ConfigLoaderTest` 14 项全绿；冒烟 `java -jar ... --config /nonexistent` 输出「配置错误: 配置文件不存在」且退出码 1
- [x] C2 对话历史 —— 证据：`ConversationTest` 7 项全绿（顺序/请求构造/标题摘要/不可变视图）
- [x] C3 SSE 解析 —— 证据：`SseParserTest` 8 项全绿（anthropic/openai 样例、多行 data、CRLF、注释、增量）
- [x] C4 双协议客户端（AC6/AC7）—— 证据：`AnthropicClientTest` 5 + `OpenAiClientTest` 4 全绿（事件序列、thinking 配置、signature 回传、401/500/坏 JSON→Error）
- [x] C5 会话存取（AC11/N4）—— 证据：`SessionStoreTest` 9 全绿（往返、损坏跳过、无 .tmp、JSON 无 apiKey、目录自建、id 格式）
- [x] C6 终端交互（AC2/AC8）—— 证据：`SlashCommandsTest` 3 全绿；PTY 冒烟：启动渲染 banner/提示符、`/exit` 退出码 0
- [x] C7 状态机与流式渲染（AC1/AC3）—— 证据：`StreamingIntegrationTest` 4 全绿；流式增量事件经 TurnRunner 回调渲染（正文/思考分色，代码审查）

## 集成

- [x] I1 Provider 工厂 —— 证据：`LlmClientFactoryTest` 3 全绿；ChatApp 通过工厂创建，不感知协议差异（代码审查）
- [x] I2 保存链路 —— 证据：集成测试断言会话文件生成且 messageCount 递增
- [x] I3 恢复链路 —— 证据：集成测试第二轮请求体包含上一轮 assistant 内容（多轮上下文回传）
- [x] I4 公开接口均有调用方 —— 证据：`mvn clean test` 57 项全绿，无死代码告警

## 编译与测试

- [x] B1 编译 —— `mvn clean compile` 无错误
- [x] B2 测试 —— `mvn test`：**57 run / 0 failure / 0 error / 0 skip**
- [x] B3 打包 —— `mvn package` 产出 `target/zhu-code-agent.jar`（4.3MB fat jar），`java -jar` 可启动（PTY 冒烟退出码 0）
- [x] B4 质量 —— M1 未引入 lint 插件，以单测 + 集成测试 + code review 保证（记录在案）

## 端到端场景

- [x] E1 完整对话闭环（mock 版）—— 证据：`openAiTurnStreamsSavesAndRestores`（流式回复→落盘→恢复→多轮回传）
- [x] E1 手动真机版（DeepSeek）—— 2026-08-07 真实 API 验证通过：流式输出、两轮多轮记忆（"我刚才问的什么"正确回答）、会话落盘 4 条消息、JSON 无 api_key、退出码 0（日志：/tmp/real-test.log）
- [x] E2 坏配置 —— `--config /nonexistent` 可读错误 + 退出码 1，不崩溃
- [x] E3 密钥脱敏 —— `sessionJsonNeverContainsApiKey` + `errorMessageNeverContainsApiKey` 全绿；会话 JSON 仅存 ProviderSnapshot（代码审查）
- [x] E4 thinking 展示 —— `anthropicThinkingSeparatedAndPersisted`：thinking 与正文分离、signature 捕获并随会话持久化；ChatApp 将 ThinkingDelta 渲染为灰字（代码审查）
- [x] E5 provider 选择 —— 单 provider 直进（PTY 冒烟）；多 provider 选择页由 JLinePicker 实现（方向键/Enter/Ctrl+C，代码审查 + 测试覆盖命令层）

## 遗留项（非阻塞）

1. Anthropic 真机对话：暂无密钥，代码路径已由 mock 测试覆盖；拿到密钥后配置 `api_key: ${ANTHROPIC_API_KEY}` 即可体验 thinking 灰字。
2. 多 provider 选择页与 thinking 灰字颜色：建议在真实终端人工目检一次（自动化已覆盖逻辑层）。
3. JDK 24+ 的 JLine native-access 警告：无害，可用 `--enable-native-access=ALL-UNNAMED` 消除（已写入 README）。

## 结论

M1「聊天 TUI + 会话持久化」验收通过，四文档（spec/plan/task/checklist）已归档至 `docs/milestones/m1/`，可进入 M2（Agent 循环与 Tool Use）规划。
