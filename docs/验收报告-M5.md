# M5 验收报告：并行与 Subagents

> 开发批（T1–T11）完成，`mvn clean test` **245/245 全绿**（M1–M4 的 215 不回归 + M5 新增 30）。
> 状态：M5 已完成（2026-08-13），分支 `feature-m5-agent-parallel`（待合并）；四文档归档 `docs/milestones/m5/`。真机 Demo 已按 `docs/demo-M5并行与Subagents.md` 完整实测通过（主链路/并行子任务/权限/级联中断）。

## 通过（41/41 + Review 修复 5 项）

### 实现完整性（10/10）
- [x] F1 StreamState 体系（基类 + Anthropic/OpenAi 子类）— 证据：`StreamState.java` + `ConcurrentStreamTest`
- [x] F1 Anthropic/OpenAiClient 去实例字段 — 证据：代码走查 + 客户端测试 26/26
- [x] F1 LlmClientFactory 缓存复用 — 证据：`ConcurrentStreamTest.factoryCachesInstanceByName`（assertSame）
- [x] F2 AbstractToolExecutor + Serial 继承 — 证据：`SerialToolExecutorTest`/`PlanModeExecutorTest`/`FileToolsTest` 全绿（行为不回归）
- [x] F2 ParallelToolExecutor — 证据：`ParallelToolExecutorTest`（保序/并行/串行/失败隔离/中断）
- [x] F3 TaskTool + ToolRegistry.registerExternal — 证据：`TaskToolTest` 6/6
- [x] F3 AgentRunner.runSubtask + SUBTASK_SYSTEM_PROMPT — 证据：`AgentRunnerTest`（runSubtask 独立会话/步数上限）
- [x] F3 SubagentCoordinator + TurnInterruptController 级联 — 证据：`SubagentIntegrationTest.interruptCascadesToSubtasks`
- [x] F3 AgentUi 子任务钩子 + TuiAgentUi 折叠单行 — 证据：编译 + 集成测试 + 真机
- [x] 配置 agent 护栏（2/4/30）— 证据：`ConfigLoaderTest`（默认/覆盖/非法回退）

### F1 客户端并发安全（AC1，4/4）
- [x] 同一实例并发两流互不串扰 — `ConcurrentStreamTest.concurrentStreamsOnSameInstanceDoNotInterfere`
- [x] cancel 不影响另一流 — `ConcurrentStreamTest.cancellingOneStreamDoesNotAffectAnother`
- [x] cancelled 不残留（实例复用）— `ConcurrentStreamTest.cancelledFlagDoesNotLeakAfterInstanceReuse`
- [x] 父+子共享实例并发 — `SubagentIntegrationTest`（真机并行子任务共享实例并发 stream）

### F2 并行工具执行（AC2，7/7）
- [x] `[R1,R2,W1,R3]` 结果保序 — `ParallelToolExecutorTest.resultsPreserveOriginalCallOrder`
- [x] 读段并行（耗时 < 串行）— `readSegmentRunsInParallelFasterThanSerial`
- [x] 写/bash 串行保持原序 — `writeToolsRunSeriallyInOriginalOrder`
- [x] 单失败隔离 — `singleFailureDoesNotFailWholeSegment`
- [x] Ctrl+C 中断在途读段 — `interruptDuringParallelSegmentDiscardsPendingResults` + `interruptStopsRemainingCalls`
- [x] 权限与串行一致（读自动/写确认/危险强制）— `executeOne` 共用逻辑 + 权限测试回归
- [x] 快照/回滚不回归（写串行快照、只读不触发）— `FileHistoryTest` + `SerialToolExecutorTest`

### F3 Subagents/Task（AC3，9/9）
- [x] 父调 task → 独立会话执行 → 摘要回填 → 父继续 — `anthropicParentSpawnsSubtaskGetsSummaryThenContinues`
- [x] 摘要回填走 tool_result、交替纪律、双协议 — `SubagentIntegrationTest`（Anthropic + OpenAI）
- [x] 并行子任务 — `openAiParallelSubtasksRunConcurrently`（真机复现）
- [x] 深度超限拒绝（工具池裁剪 + 兜底）— `depthLimitPrunesTaskToolFromSubtaskToolset` + `nestedDepthPrunesTaskAtGrandchild`（R1）
- [x] 父已批准不二次确认 — `parentApprovedPermissionInheritedBySubtask`
- [x] 未批准回主 UI 确认 + 来源标注 — 真机 `[子任务#N] 请求权限` + mock 断言
- [x] 级联中断、摘要「被中断」— `interruptCascadesToSubtasks` + 真机
- [x] 父计数只计 task 本身 — `parentStepBudgetCountsTaskCallOnce`
- [x] 失败不自动重试 — `TaskToolTest.subtaskFailureReturnsErrorSummaryNoAutoRetry`

### N2/N3/N4 安全兼容稳定（AC4，5/5）
- [x] 安全红线不削弱（危险强制/禁写目录/密钥脱敏）— 现有安全测试全绿 + 子任务路径走查
- [x] 双协议回填不回归 — `AgentLoopIntegrationTest` + `SubagentIntegrationTest` 双协议全绿
- [x] 子任务摘要回填交替纪律 — `SubagentIntegrationTest` 消息序断言
- [x] M4 占用/压缩/中断/JSONL 不回归 — `M4ContextIntegrationTest`/`SessionStoreJsonlTest` 全绿
- [x] 失败路径不崩溃 — 各失败用例 + 全量测试

### 编译与测试（AC5，2/2）
- [x] `mvn clean test` 245/245 全绿（BUILD SUCCESS）
- [x] pom.xml 无变更（N1 零新依赖）+ 新增测试 6 类齐备

### 端到端场景（3/3）
- [x] 场景 1（主流程）：父派 2 并行子任务 → 摘要回填 → 父综合 → JSONL 落盘恢复、无 api_key — `SubagentIntegrationTest` + 真机
- [x] 场景 2（边界）：孙 agent 再派 task 被拒（工具池裁剪）→ 可读错误、不崩溃 — `nestedDepthPrunesTaskAtGrandchild`
- [x] 场景 3（真机 PTY）：子任务写文件确认弹窗带 `[子任务#N]` + 串行；Ctrl+C 级联中断「被中断」— `docs/demo-M5并行与Subagents.md` 冒烟记录

### Review 修复（R1–R7，5 项代码 + 2 项真机）
- [x] R1 深度护栏（AgentDepth 显式传播）— `nestedDepthPrunesTaskAtGrandchild` 红→绿
- [x] R2 /plan 拦截 task — `PlanModeExecutorTest.taskBlockedInPlanMode` 红→绿
- [x] R3 终端输出锁 — TerminalUi outputLock + 全量回归
- [x] R4 中断 shutdownNow — ParallelToolExecutorTest 回归
- [x] R5 并行 bash 进程销毁 — `BashToolParallelInterruptTest`（3 并行 sleep cancel 全销毁）
- [x] R6 真机 SIGINT rearm — TurnInterruptControllerTest + 集成回归（真机复验通过）
- [x] R7 工具摘要换行 — textOpen + 集成回归（真机复验通过）
